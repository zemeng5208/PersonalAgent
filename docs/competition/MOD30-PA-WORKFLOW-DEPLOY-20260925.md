# MOD-30：通用受限协议云配置与原实例部署读回

Profile：`huawei_ict_agentarts`；负责人：zemeng；区域：`cn-southwest-2`。
2026-09-25 在现有 AgentArts 应用中操作，仅替换证据安全审查节点及多 Agent
控制器的系统提示词；旧版全文见
[`MOD30-PA-WORKFLOW-RESTORE.md`](MOD30-PA-WORKFLOW-RESTORE.md)，
新版全文见 [`MOD30-PA-WORKFLOW-PROMPT-V2.md`](MOD30-PA-WORKFLOW-PROMPT-V2.md)。
云端未新增第二个应用或运行实例，未改起始世界分析、默认计划修复的版本引用。

| 阶段 | 控制台实际读回 |
| --- | --- |
| 证据安全审查 | 原工作流 `998031d7-ec28-40ed-83e6-96667f22ff51` 的节点提示词读回含通用 A/B/C 规则；发布成功，版本名 `v20260925191806`，数值版本 ID `1790335103598`。子工作流自己的实例部署弹窗已取消。 |
| 主控草稿 | 原多 Agent 应用 `32d4d44c-eade-4f3f-8f76-209c74609e79` 的提示词刷新后读回为通用目标版；画布上两个审查引用均读回 `1790335103598`。起始世界分析引用 `1789532410207`，默认计划修复引用 `1789532322503`，均未改变。 |
| 主控发布 | 发布版本成功，版本名 `v20260925192219`；渠道管理显示 API“已发布”，时间 2026-09-25 19:22:30。 |
| 实例部署 | 发布时选择原运行实例 `agent-arts-d5ae1174bc7d4cb8ab3dbbc6fae654e4`，平台显示“部署成功”。实例列表及详情独立读回同一实例 ID `1beeb366-7d05-4bb5-b054-d0b626aaef7f`、状态“正常”、更新时间 2026-09-25 19:22:49。`Latest` 指向运行时版本 `v8`，历史 `v7` 仍列在版本页。入站仍为 API Key 认证。 |

主控修改期间两次出现“智能体应用已被人修改，请刷新页面后再试”提示。
每次刷新后服务端画布均读回已保存的提示词或新引用；最终发布前两个引用
同为 `1790335103598`，因此未据提示直接判定成功或失败。平台“发布版本成功”、
渠道 API“已发布”、实例“正常／Latest v8”是不同层级的读回。随后 MOD-04B
执行单次非会议合成协议探针；平台观测页可见 2026-09-25 19:30:13 GMT+08:00
的多智能体成功记录，Trace ID `668cded1dd4cb0de4a38bae90cf9736d`，
含三段 UserInput 调用树。该记录与 MOD-04B 本地回执的时间和输入相符，
但当前页面尚未直接显示请求使用的部署版本；请求级 span 由 MOD-29 的唯一
平台记录核对者继续确认。此探针只覆盖受限协议解析，不证明本地工具、
图写入、普通目标完整闭环或旧会议路径在 v8 上回归。

回退参考：主控原实例运行时 `v7`；审查原数值版本 ID `1790261079291`；
旧提示词与路由见恢复包。若需回退，应由同一云配置持有者先确认当前实例
绑定，再通过平台版本选择部署并独立读回；不以本记录代替回退操作。

下一验收：与 MOD-04B 协调仅一次非会议合成协议实调，使用
[`MOD30-ORDINARY-GOAL-CONTRACT.md`](MOD30-ORDINARY-GOAL-CONTRACT.md)
中的单个 `workspace.read_text@1.0.0` 相对路径目标，核对云输出形状、本地
Policy/ToolGateway 及目标读回；需图修复时先绑定真实合成图快照。该测试
不重复已通过的会议完整主链。非作者评审及集成结果另行记录。

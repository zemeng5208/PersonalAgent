# Competition 跨任务成果提交说明

负责人 zemeng；评审 goo122；profile `huawei_ict_agentarts`；状态 review。

本整合分支将已指导任务的 Desktop、语音、工作区工具和 AgentArts 接线成果集中送审，
不是新架构，也不宣称整个模块或比赛 Golden Path 完成。已有独立 PR 保留；评审时先核对
分支祖先关系，不能把整合 PR 与其依赖当互不相关的实现重复合并。

## 覆盖关系

- MOD-18：工作区 read/list/patch preview 与 Competition 只读集成，保留现有 PR63、68、69。
- MOD-14/15：会话、Runtime 消费者、唤醒生命周期及组合，保留现有 PR61、65、70、75；
  本分支另含 PCM/WAVE、System.Speech 适配及其 PowerShell 语法与子进程环境隔离修复。
- Desktop：工作区授权/撤销、工具注册、Workflow 启动、按住说话接线。
- AgentArts：包含 PR51、72 的文字与 Workflow 输入修复，以及 MCP 到批准过的本地
  Runtime 工具的首片桥接。已有 PR49 的离线工具循环也是依赖，不冒充真实云工具循环。
- Chat《架构建议稿》：以下为其可交付建议的落盘摘要，不是冻结接口或验收事实。

## Chat 建议：最小比赛验收叙事

仅用合成会议数据：会议15点、提前1小时准备、另有无关计划。真实 AgentArts 选择只读
工具，本机 Policy 校验许可，Runtime 执行并记录 Evidence；获准的最小结果回传云端。
随后将合成会议改为17点，形成新事实版本，定位受影响依赖并提出最小修复；本地校验
版本及确认后提交，读回准备时间14→16点、无关计划版本不变，不自动改用户文件。

证据需绑定任务、部署版本、云请求、审批范围、工具读回、Evidence、前后事实/计划版本。
云失败、续接未完成、越权、读回缺失、版本冲突或误改无关计划时，明确报告端到端未完成。
建议中的交接分类案例（本地许可不等于出机许可、换 Agent 不重置 allow_once、只保留
已有证据支持的结论）仅属评估设计，不是已运行测试或工具安全证明。

## 已有验证与限制

- 桥接及 Runtime invoker 联合测试 Node24.15：5/5；含实际 loopback HTTP、真实本地
  Runtime/Policy/ToolGateway 和合成文件读回，详见 MOD-30-LOCAL-MCP-BRIDGE-01.md。
- 已记录 Desktop workspace-access 6/6、PTT 4/4、语音 Fake 进程适配3/3，不能据此
  宣称真实麦克风、扬声器或云端端到端成功。构建环境差异见各工作包记录。
- 单次控制台合成验收已留档；云响应不等于本地执行完成，语义限制见对应记录。
- 尚无公网 MCP 入口，用户仅允许准备临时 HTTPS 接入；不得据此立即开放电脑。
- 尚未跑本整合头的全量 `npm run check`；公共集成合并前须通过必要门禁并获非作者批准。
- 依赖中 SSE 夹具的 EOF 空行保留事件分隔，差异检查告警已知，不虚报全量通过。
- 历史文档中的“未推送”描述对应当时检查点；当前发布状态以本 PR 为准。

所有这些是代码/文档交付，不是发布安装包、冻结生产接口或宣布比赛验收完成。

# 协作开发规范

版本：0.2 · 更新：2026-09-05 · 状态：多人模块协作基线

## 1. 决策与职责

| 角色 | 责任 |
| --- | --- |
| 产品负责人（用户） | 产品目标、关键交互、范围与重大取舍 |
| `goo122`（A） | 工程底座、公共协议、根配置和集成；Obsidian 与智能核心 |
| `zemeng`（B） | 桌面、语音、Windows 执行等已分配模块的实现与验证 |
| `Potatos498`（C） | 可独立领取的连接器与信息业务模块 |
| 模块负责人 | 在分配范围内实现、验证、维护文档、报告阻碍 |
| 评审者 | 对照需求、接口、权限边界及验收证据审查 |

各模块负责人可以决定范围内普通实现细节；产品边界、真实账号授权、不可逆操作和重大架构变化按已有授权处理。用户名不代表任何工具已经连通。所有协作者使用同一验收标准。

模块归属唯一来源是 [模块分工](docs/MODULE_ASSIGNMENTS.md)，公共契约语义见 [公共开发协议](docs/DEVELOPMENT_PROTOCOL.md)。平台子任务认领按分工文档执行；开始开发前必须登记 GitHub 用户名。

## 2. 需求与任务流

流程：需求编号 → 工作包 → 接口确认 → 实现 → 关键验证 → PR → 评审 → 集成 → 更新进度。

任务状态：`todo`、`ready`、`in_progress`、`review`、`blocked`、`done`。

- `ready`：输入、接口依赖和验收条件明确。
- `blocked`：记录具体原因、已取得证据和解阻条件。
- `done`：已集成且满足约定验收条件。只完成代码但尚未验证，留在 `review`。

每个工作包按以下格式登记在 `docs/ROADMAP.md`；规模增长后可迁移到统一任务系统并保留链接，不同时维护冲突的状态副本。

```text
任务 ID / 标题：
模块 ID / 关联需求：MOD-xx / PA-xxx
负责人 / 评审者：
状态：
拥有的模块或文件：
输入与依赖：
交付物及不在范围内的内容：
验收条件与验证方法：
结果证据 / PR：
剩余问题与继续入口：
```

## 3. 文件所有权与并行工作

- 一个工作包只设一个交付负责人。多个独立工作包可以并行，同一共享文件不得多人同时编辑。
- 公共协议、数据库迁移、根依赖锁文件和根启动装配由 `goo122` 协调；模块内部入口归模块负责人。新增模块导出 `register(host)`，由 `goo122` 接入根装配。
- Agent 委派消息必须包含文件所有权、验收条件，并声明“你不是唯一协作者，不要覆盖他人改动”。
- 共用一个工作区时采用串行编辑；并行实现使用独立 checkout/worktree。项目相关工作树建议放在项目内 `.worktrees/`，实际启用时配置忽略规则。
- 每个工作树独立安装和运行，避免共享可写数据库、测试账号状态及端口。
- 不使用 `reset --hard`、强制推送或清理未跟踪文件来解决普通冲突。
- 完成工作后交接接口变更、验证结果和剩余风险，不只发送“完成”。

## 4. Git 与 PR

- 主分支保持可集成。分支命名：`feat/任务号-主题`、`fix/任务号-主题`、`docs/主题`、`chore/主题`。
- 一个 PR 对应一个可独立审查的工作包，避免混入格式化、依赖升级和无关修改。
- 提交建议使用 `feat(runtime): ...`、`fix(desktop): ...`、`docs(prd): ...`；描述具体行为。
- PR 使用仓库模板，关联需求、说明实际验证和限制。没有执行的检查明确写“未执行”。
- 合并前至少有一名非作者评审；`goo122` 的 PR 由 `zemeng` 或已登记的第三位协作者评审，`zemeng` 的 PR 由 `goo122` 或已登记的第三位协作者评审。无人可评审时留在 review，不标记为已通过他人评审。
- 建议合并策略为 squash；是否开启分支保护、CI 和谁有远端权限仍需实际配置，本文不表示已启用。
- PR 合并不等于发布。发布和真实账号接入按用户授权范围另行执行。

## 5. 接口与代码规则

- TypeScript 使用严格类型检查；共享数据先定义契约，再写调用方。外部输入在运行时校验。
- 跨进程消息必须含协议版本、请求 ID、任务 ID（适用时）、操作类型和结构化结果。
- 统一错误至少区分：输入错误、未授权、不支持、限流、超时、外部失败、结果不确定。
- 工具能力声明包含副作用、是否可重试、是否可恢复、数据范围、是否需要用户在场；权限系统核验这些声明。
- 账号凭据不放进配置示例、前端状态、提示词、数据库明文字段或日志。
- 不为“所有平台”写分支堆叠：新增平台实现连接器契约，业务层只依赖能力。
- 数据库变更使用有序迁移；执行前保护现有数据，不能用清库代替迁移。
- 引入依赖说明用途、打包成本、许可证兼容性和替代方案；不因此擅自为项目添加许可证文件。
- 不提前规定不存在的代码工具命令。工程建立后，脚本与 CI 共用实际配置。

## 6. 测试与验证

采用少量关键测试；不为低风险改动编写重复实现逻辑的测试。

| 改动 | 必需验证重点 |
| --- | --- |
| 文档 | 相对链接、编号一致性、状态和措辞 |
| Runtime | 一个完整任务闭环、取消或中断恢复、关键失败处理 |
| 权限与写操作 | 授权范围、越权拒绝、不确定结果不重复写入 |
| 连接器 | 契约校验；具备账号后再做真实读回；模拟与真实结果分开 |
| 电脑操作 | 目标确认、用户接管、后置结果；真实 Windows 交互 |
| UI/语音 | 实际渲染、缩放/多屏关键场景、输入打断、状态一致性 |
| 打包发布 | 干净环境安装、启动、核心功能、卸载与数据保留策略 |

模拟测试只验证程序分支，不能证明第三方平台已连通。编译通过不能证明 UI、语音或电脑控制可用。

## 7. 完成标准

- 关联需求的验收条件满足，明确适用环境。
- 代码与共享契约一致，必要文档同步。
- 关键验证通过，有可重现步骤或 CI 证据。
- 相关运行日志可定位任务，但不暴露敏感内容。
- 已知限制、未验证平台和恢复方式已记录。
- PR 已评审与集成，任务状态更新。

证据可标注：`verified`（本轮验证）、`conditional`（条件成立才有效）、`draft`（设计/模拟）。不要把后两者写成完成能力。

## 8. 需求变更与暂停交接

产品行为变更先更新 PRD；跨模块架构变化在 ARCHITECTURE 的决策记录中写明原因、影响、替代方案和迁移方式。`goo122` 协调公共接口变更并通知依赖模块；变更模块负责人先更新 MODULE_ASSIGNMENTS，避免重复所有权。

暂停时保存：当前分支、相关提交或未提交文件、最后通过的验证、失败证据、下一步。恢复先读现有记录，不重新创建同一任务。

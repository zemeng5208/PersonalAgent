# 贡献指南

## 分支

从最新的 `main` 创建功能分支，建议使用以下命名格式：

- `feature/<简短描述>`：新增功能
- `fix/<简短描述>`：修复问题
- `docs/<简短描述>`：仅文档修改
- `chore/<简短描述>`：工程维护

## 提交

每次提交只包含一个完整、可理解的变更。提交信息使用动词开头，并说明变更目的，例如：

```text
Add project initialization files
Fix agent configuration loading
```

## Pull Request

Pull Request 至少应包含：

1. 变更目的和范围；
2. 验证方式及结果；
3. 对配置、数据迁移或兼容性的影响；
4. 需要审阅者重点关注的内容。

不要把无关的格式化、重命名或清理混入同一个 Pull Request。

## 安全

密钥和本地配置只保存在未纳入版本控制的环境文件或密钥管理系统中。发现安全问题时，不要直接在公开 Issue 中发布敏感细节。
